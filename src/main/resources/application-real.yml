spring:
  profiles:
    include: real-db
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

logging:
  level:
    com:
      example:
        colorproject: info