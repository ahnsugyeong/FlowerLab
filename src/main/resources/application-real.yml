spring:
  profiles:
    include: real-db
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

logging:
  level:
    com:
      example:
        colorproject: info